# ── Referrals Service — variáveis de ambiente ──────────────────────────────
# Copie este arquivo para .env e preencha os valores

NODE_ENV=development

PORT=3003

# PostgreSQL — local (Docker):
DATABASE_URL=postgresql://empregabilidade:senha_segura_123@localhost:5432/empregabilidade_db

# PostgreSQL — Supabase (Transaction Pooler, recomendado):
# DATABASE_URL=postgresql://postgres.[SEU-PROJECT-REF]:[SUA-SENHA]@aws-0-[REGIAO].pooler.supabase.com:6543/postgres
#
# PostgreSQL — Supabase (Direct Connection):
# DATABASE_URL=postgresql://postgres:[SUA-SENHA]@db.[SEU-PROJECT-REF].supabase.co:5432/postgres

# CORS — URL do frontend em produção (deixar vazio em dev para permitir todas as origens)
FRONTEND_URL=
